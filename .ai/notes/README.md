# Notes

Use this folder for short-lived observations, experiments, and reminders.

## Rules
- Keep notes brief.
- Do not use notes as the source of truth for product direction.
- If a note becomes important, move it into `project/`, `roadmap/`, or `memory/`.

